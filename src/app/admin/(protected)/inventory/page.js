import StateCard from "@/components/admin/StateCard";

export default function AdminInventoryPage() {
	return (
		<StateCard
			title="Inventory Moving Next"
			message="Trailer inventory management is queued for the operations migration phase. The new platform foundation is in place, and this screen will return on the tenant-first architecture."
		/>
	);
}
