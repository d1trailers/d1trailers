import StateCard from "@/components/admin/StateCard";

export default function AdminRentalsPage() {
	return (
		<StateCard
			title="Rentals Moving Next"
			message="The new rental operations surface will be rebuilt on the tenant platform in the next shift. The foundational rental tables are in place, but this screen is intentionally held until the operations migration phase."
		/>
	);
}
