import StateCard from "@/components/admin/StateCard";

export default function AdminWatchlistPage() {
	return (
		<StateCard
			title="Watchlist Moving Next"
			message="Billing watchlist workflows will return once the operational and billing layers are rebuilt on the new tenant platform."
		/>
	);
}
